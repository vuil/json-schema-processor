Use this image to process a json schema file to markdown and console-readable
text.

expects the SCHEMA environment variable to point to the path of the json schema
file accessible by the container.

